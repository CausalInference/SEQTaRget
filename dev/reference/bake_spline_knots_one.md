# Bake fixed knots into a single formula string

Bake fixed knots into a single formula string

## Usage

``` r
bake_spline_knots_one(covs, data)
```

## Arguments

- covs:

  single RHS formula string

- data:

  `data.table` holding the columns the model is fit on
