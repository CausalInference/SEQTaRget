# Strip large components from a model object returned by fit_glm

Strip large components from a model object returned by fit_glm

## Usage

``` r
clean_fastglm(model)
```

## Arguments

- model:

  a model object (fastglm or parglm.fit)
