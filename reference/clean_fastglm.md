# Function to clean out non needed elements from fastglm return

Function to clean out non needed elements from fastglm return

## Usage

``` r
clean_fastglm(model)
```

## Arguments

- model:

  a fastglm model
