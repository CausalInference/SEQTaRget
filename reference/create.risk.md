# Internal function to pull Risk Ratio and Risk Difference from data when `km.curves = TRUE`

Internal function to pull Risk Ratio and Risk Difference from data when
`km.curves = TRUE`

## Usage

``` r
create.risk(data, params)
```
