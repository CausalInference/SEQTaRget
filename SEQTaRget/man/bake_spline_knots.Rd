% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/internal_misc.R
\name{bake_spline_knots}
\alias{bake_spline_knots}
\title{Bake fixed knots into every \code{ns(x, df = N)} term of a formula string}
\usage{
bake_spline_knots(covs, data)
}
\arguments{
\item{covs}{character vector of RHS formula strings (may be \code{NA})}

\item{data}{\code{data.table} holding the columns the model is fit on}
}
\value{
\code{covs} with every bakeable \code{ns()} term rewritten
}
\description{
\code{splines::ns()} recomputes its knots from whatever data it sees, so without
fixed knots the basis built at prediction time differs from the one used at
fit time (the weight and outcome models are fit on, and predicted over,
different row subsets). This helper rewrites every \code{ns(x, df = N)} token in
\code{covs} to an explicit \code{ns(x, knots = c(...), Boundary.knots = c(...))},
with the knots computed once from the full \code{x} column of \code{data} - the
quantiles \code{ns()} itself would use. The result is a formula whose
\code{model.matrix} output is invariant to the row subset passed in, and constant
across bootstrap resamples.
}
\details{
Tokens are left unchanged when the variable is absent from \code{data} or is not
numeric, and when the term already carries explicit knots (only the
\code{df = N} form is rewritten), so user-supplied bases pass through untouched.
}
\keyword{internal}
