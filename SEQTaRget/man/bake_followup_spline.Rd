% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/internal_misc.R
\name{bake_followup_spline}
\alias{bake_followup_spline}
\title{Bake fixed knots into any \code{ns(followup, df = N)} term in \code{params@covariates}}
\usage{
bake_followup_spline(params)
}
\description{
\code{splines::ns()} recomputes knots from whatever data it sees, so without fixed
knots the basis at prediction time differs from the basis used at fit time.
This helper rewrites every \code{ns(followup, df = N)} token in
\code{params@covariates} to an explicit \code{ns(followup, knots = c(...), Boundary.knots = c(...))}
computed once from the full expanded \code{followup} column. The result is a
formula whose \code{model.matrix} output is invariant to the row subset passed in.
}
\details{
Returns the original covariates string unchanged when no \code{ns(followup, df = ...)}
token is present (e.g. user supplied custom covariates that already specify
knots).
}
\keyword{internal}
